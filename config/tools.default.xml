<?xml version="1.0" encoding="UTF-8"?>
<tools>
  <!-- Built-in tools (cannot be edited or deleted — only toggled on/off) -->
  <tool>
    <name>help</name>
    <api>ollama</api>
    <timeout>120</timeout>
    <description>Explain what the bot can help with and how to use each capability</description>
    <builtin>true</builtin>
    <allowEmptyContent>true</allowEmptyContent>
  </tool>

  <tool>
    <name>activity_key</name>
    <api>ollama</api>
    <timeout>10</timeout>
    <description>Request a temporary access key for the activity monitor</description>
    <builtin>true</builtin>
    <allowEmptyContent>true</allowEmptyContent>
  </tool>

  <!-- Image generation tool -->
  <tool>
    <name>generate_image</name>
    <api>comfyui</api>
    <timeout>600</timeout>
    <description>Generate image using ComfyUI</description>
    <abilityWhen>User wants an image generated.</abilityWhen>
    <parameters>
      <mode>implicit</mode>
      <prompt>
        <type>string</type>
        <description>Detailed text description of the desired image. Infer from reply target or current message if possible.</description>
        <required>true</required>
      </prompt>
      <negative_prompt>
        <type>string</type>
        <description>Terms to exclude from the generated image (e.g. "blurry, low quality, watermark"). Optional.</description>
        <required>false</required>
      </negative_prompt>
      <inferFrom>reply_target, current_message</inferFrom>
      <validation>Use the reply target text if present; otherwise use descriptive text from the current message. If no usable image prompt text can be inferred, ask the user what they want generated.</validation>
      <examples>
        <example>generate_image a sunset over mountains</example>
      </examples>
    </parameters>
  </tool>



  <!-- Weather tool -->
  <tool>
    <name>get_current_weather</name>
    <api>accuweather</api>
    <timeout>60</timeout>
    <description>Get weather details including current conditions and forecast</description>
    <abilityWhen>User asks about weather details for a location, including temperature, precipitation, wind, and multi-day outlook.</abilityWhen>
    <parameters>
      <mode>explicit</mode>
      <location>
        <type>string</type>
        <description>City name, region, or US postal code to look up weather for</description>
        <required>true</required>
      </location>
      <validation>Location must be a valid worldwide city name, region, or United States postal code.</validation>
      <examples>
        <example>get_current_weather Dallas</example>
        <example>get_current_weather 90210</example>
        <example>get_current_weather London, UK</example>
      </examples>
    </parameters>
    <contextFilterMaxDepth>1</contextFilterMaxDepth>
  </tool>

  <!-- NFL tool -->
  <tool>
    <name>get_recent_nfl_data</name>
    <api>nfl</api>
    <timeout>30</timeout>
    <description>Get recent NFL scores or news. Provide a data type: "scores" for game results, "news" for headlines. Optionally add a filter — a date (YYYYMMDD or YYYY-MM-DD) for scores, or a keyword (e.g. team name) for news.</description>
    <abilityWhen>User asks about NFL scores, game results, news, or headlines.</abilityWhen>
    <parameters>
      <mode>mixed</mode>
      <dataType>
        <type>string</type>
        <description>The kind of NFL data to retrieve: "scores" or "news".</description>
        <required>true</required>
      </dataType>
      <filter>
        <type>string</type>
        <description>Optional filter — date (YYYYMMDD or YYYY-MM-DD) for scores, keyword/team name for news.</description>
        <required>false</required>
      </filter>
      <inferFrom>current_message</inferFrom>
      <validation>Type must be "scores" or "news". Filter is optional: for scores a date in YYYYMMDD or YYYY-MM-DD format; for news a keyword to narrow results.</validation>
    </parameters>
    <allowEmptyContent>true</allowEmptyContent>
  </tool>



  <!-- Search tools -->
  <tool>
    <name>web_search</name>
    <api>serpapi</api>
    <timeout>60</timeout>
    <description>Search the web for current information using Google</description>
    <abilityWhen>User needs current or real-time information from the web.</abilityWhen>
    <parameters>
      <mode>explicit</mode>
      <query>
        <type>string</type>
        <description>Concise search term or question suitable for a web search engine</description>
        <required>true</required>
      </query>
      <inferFrom>current_message</inferFrom>
      <validation>Query should be a concise search term or question suitable for a web search engine.</validation>
      <examples>
        <example>web_search latest TypeScript release</example>
        <example>web_search Super Bowl winner 2026</example>
      </examples>
    </parameters>
    <contextFilterMaxDepth>1</contextFilterMaxDepth>
  </tool>

  <!-- Meme tools -->
  <tool>
    <name>generate_meme</name>
    <api>meme</api>
    <timeout>60</timeout>
    <description>Create meme images from popular templates</description>
    <abilityWhen>User asks to create or generate a meme image.</abilityWhen>
    <parameters>
      <mode>implicit</mode>
      <template_and_text>
        <type>string</type>
        <description>Output must be: templateName | top text | bottom text. Choose the most fitting meme template for the user's intent.</description>
        <required>true</required>
      </template_and_text>
      <inferFrom>current_message, reply_target</inferFrom>
      <validation>Output must be: templateName | top text | bottom text. Choose the most fitting meme template for the user's intent. If the user names a specific template, use it; otherwise pick the best match.</validation>
      <examples>
        <example>generate_meme success kid | finished all my tasks | on a Monday</example>
        <example>generate_meme one does not simply | walk into mordor</example>
      </examples>
    </parameters>
    <contextFilterMaxDepth>1</contextFilterMaxDepth>
  </tool>

  <tool>
    <name>get_meme_templates</name>
    <api>meme</api>
    <timeout>30</timeout>
    <description>Return a comma separated list of meme templates</description>
    <allowEmptyContent>true</allowEmptyContent>
  </tool>
</tools>
