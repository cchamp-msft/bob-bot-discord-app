# Docker-specific environment overrides for running bob-bot in containers.
#
# Usage:
#   1) Start from .env.example -> .env
#   2) Copy values from this file that match your Docker deployment
#
# Keep secrets out of source control.

# ---------- Container networking ----------
# Inside a container, localhost is the container itself.
# Docker Desktop (Windows/Mac): use host.docker.internal for host services.
OLLAMA_ENDPOINT=http://host.docker.internal:11434
COMFYUI_ENDPOINT=http://host.docker.internal:8190

# Bind configurator inside container so mapped host port can reach it.
HTTP_HOST=0.0.0.0

# Keep outputs server listening on all interfaces in container.
OUTPUTS_HOST=0.0.0.0

# ---------- Reverse proxy / TLS ----------
# Public base URL used in generated links (must be externally reachable).
# For HTTPS behind reverse proxy, use your external URL.
OUTPUT_BASE_URL=https://bot.example.com

# Trust exactly one proxy hop (recommended for single Nginx/Caddy/Traefik layer).
# Use false when not behind a trusted proxy.
OUTPUTS_TRUST_PROXY=1

# ---------- Remote configurator hardening ----------
# Enable only if you need to access configurator from outside container namespace.
CONFIGURATOR_ALLOW_REMOTE=true

# Limit allowed source IPs/CIDRs for configurator requests.
# Add Docker bridge CIDR and/or your LAN range as needed.
CONFIGURATOR_ALLOWED_IPS=172.17.0.0/16,192.168.0.0/16

# Required when CONFIGURATOR_ALLOW_REMOTE=true.
# Generate with: openssl rand -hex 32
ADMIN_TOKEN=
